DB_USER=postgres
DB_HOST=localhost
DB_NAME=Blog
DB_PASSWORD=your_password_here
DB_PORT=5432
PORT=3000
SESSION_SECRET=your_secret_here
# Optional admin gate (username & email compared case-insensitive; password must match exactly)
# Leave unset to use app defaults, or set all three to rotate the admin password.
# ADMIN_USERNAME=
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
